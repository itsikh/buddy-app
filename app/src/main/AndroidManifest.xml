<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Core network access for AI API calls and Drive sync -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Microphone for voice conversation with Buddy -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Biometric authentication for admin/parent sections -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- OTA updates via GitHub Releases -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Session reminders and progress notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Required by Google Sign-In for Drive access -->
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />

    <!-- Microphone is core functionality, not optional -->
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="true" />

    <application
        android:name=".TemplateApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.TemplateApp">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.TemplateApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            Transparent no-UI activity for SAF file picker (manual backup/restore).
            Started via a notification PendingIntent from BaseBackupManager.
        -->
        <activity
            android:name=".backup.BackupSaveActivity"
            android:exported="false"
            android:launchMode="singleInstance"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!--
            Disable the default WorkManager initializer so we can provide our own
            Hilt-aware WorkerFactory via Configuration.Provider in TemplateApplication.
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
